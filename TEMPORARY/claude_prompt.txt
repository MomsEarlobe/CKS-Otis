# Detailed Prompt for Claude

If you choose to have Claude finish the implementation, you can copy and paste the following prompt:

***

**Prompt:**

"I have a static HTML/CSS website for a commercial kitchen equipment repair business, and I need you to make several major design and structural changes across the site. Here is the detailed list of everything that needs to be fixed:

**1. Global Typography & Colors:**
- The headers (H1, H2) on all pages are currently way too big (e.g., using `clamp(2.5rem, 5vw, 4rem)`). Please significantly reduce the header sizes across the entire site to be modern but not overwhelming.
- The dark blue text used for headings and accents is hard to read against certain backgrounds. Change the text color of the main body and headings to a high-contrast dark gray/black, and reserve the brand blue strictly for buttons, hover states, and small accents.

**2. Home Page (`index.html`):**
- In the 'About' section (the split-layout intersection), change the image to the picture of Otis leaning against his truck.
- Remove the solid blank gray background from this section and replace it with a clean white background, using subtle drop-shadows on the text or image to create depth instead.

**3. FAQ Page (`faq.html`):**
- Redesign the FAQ accordion. The current clickable boxes are plain and boring. Style them as beautiful, modern interactive cards with soft box-shadows, hover-lift effects, and smooth animated dropdowns. They must be easy to read and highly interactive.

**4. Merging Blog & Gallery (`blog.html` & `gallery.html`):**
- Combine the Blog and Gallery pages into a single page called 'Blog'.
- Delete `gallery.html` entirely.
- Update the navigation bar across all pages to remove the 'Gallery' link.
- On the new 'Blog' page, integrate the gallery photos so that the pictures correlate directly with the blog posts. Keep the gallery header styling (which I liked) at the top of this new merged page.

**5. Schedule Service Page (`schedule.html`):**
- There are currently two redundant lists of 'What we service' on this page. Consolidate them into ONE single list.
- Style this single list to be highly interactive and pretty (e.g., a modern CSS grid with icons and hover effects), rather than a basic bulleted list.
- Add significant margin/padding above the footer so that the blue 'Schedule Service' button is not cramped against the bottom of the page.

**6. Contact Page (`contact.html`):**
- Reduce the size of the 'Contact Us' header.
- Ensure the text is highly readable (no dark blue text on dark backgrounds). The form and the iframe map at the bottom should remain as they are.

**7. HouseCall Pro 'Reviews' Widget:**
- The HouseCall Pro booking script injects a floating 'Read our reviews' badge on the right side of the screen on every page. This is intrusive. Please write custom CSS to target and `display: none;` that specific floating widget.
- Instead, add a static 'Read Our Reviews' link to the footer of the website on all pages.

Please provide the updated HTML for `index.html`, `faq.html`, `blog.html`, `schedule.html`, `contact.html`, and the updated `css/styles.css` file to apply these changes."
